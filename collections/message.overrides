/*******************************
         Theme Overrides
*******************************/

.ui.message {
  border-color: currentColor;
  border-left: @borderRadius solid;

  a {
    color: currentColor;
  }

  :not(pre) > code[class*="language-"],
  pre[class*="language-"] {
    background: @transparentBlack;
  }
}